/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // webpack5: true,
  // webpack: (config) => {
  //   config.resilve.fallback = { fs: false };
  //   return config;
  // },
};

module.exports = nextConfig;
